import React from 'react'

const PodcastCard = ({imgUrl, title, podcastId, description}: {imgUrl: string, title: string, description: string, podcastId: number}) => {
  return (
    <div className='curser-pointer hover:shadow-2xl hover:shadow-orange-1'></div>
  )
}

export default PodcastCard; 